name: Deploy to Tárhely.eu

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install lftp
        run: sudo apt-get install -y lftp

      - name: Deploy via FTP
        env:
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          lftp -u "somfaikl,$FTP_PASSWORD" ftp://tefiftythree.tarhely.eu <<'EOF'
          set ftp:ssl-allow no
          set ftp:passive-mode yes
          set net:timeout 30
          set net:max-retries 3
          set net:reconnect-interval-base 5
          set ftp:use-mdtm no
          mirror --reverse --delete --no-perms --verbose \
            --exclude-glob .git \
            --exclude-glob .github/ \
            --exclude-glob tests/ \
            --exclude-glob docs/ \
            --exclude-glob phpunit.xml \
            --exclude-glob composer.json \
            --exclude-glob qr-admin/includes/config.php \
            . .
          bye
          EOF
